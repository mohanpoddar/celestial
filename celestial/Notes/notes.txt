First download and extract

Step-01: Go to the directory and run below command
o - For original user while creating the os
u - creating new user ex. devopsadmin

$ bash ansible-ubuntu-setup.sh -u devopsadmin -o cce
